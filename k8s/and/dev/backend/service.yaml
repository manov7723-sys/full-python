apiVersion: v1
kind: Service
metadata:
  name: full-python-backend
  namespace: and
  labels:
    app.kubernetes.io/name: full-python-backend
    app.kubernetes.io/managed-by: deepagent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: full-python-backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
